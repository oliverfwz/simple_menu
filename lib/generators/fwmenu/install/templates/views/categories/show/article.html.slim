= "article.html.erb"
- if @category.articles.empty?
	br
	p = "No article found"
- else 
	- @category.articles.each_slice(4) do |articles|
		- articles.each do |article|
			.col-sm-3
				h2 = link_to article.title, article_path(article)
				.description = article.description
		.clear